class User {
  constructor({ userId, email, username, password }) {
    this.id = userId;
    this.email = email;
    this.username = username;
    this.password = password;
  }
}

export default User;
